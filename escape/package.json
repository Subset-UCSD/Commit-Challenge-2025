{
	"scripts": {
	  "build": "esbuild ./*.ts --outdir=. --bundle --format=esm --tree-shaking=false",
	  "dev": "esbuild ./*.ts --outdir=. --bundle --format=esm --tree-shaking=false --serve --servedir=."
	},
	"type": "module",
	"dependencies": {
	  "esbuild": "^0.24.2"
	},
	"devDependencies": {
	  "@types/express": "^5.0.0",
	  "@types/node": "^22.13.4",
	  "nodemon": "^3.1.9"
	}
}